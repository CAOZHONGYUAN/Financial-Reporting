<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fms.dao.DeptDao">
    <select id="selectDeptnoByUserid" resultType="Integer" parameterType="Integer">
        select deptId from dept where userId = #{userId}
    </select>
</mapper>